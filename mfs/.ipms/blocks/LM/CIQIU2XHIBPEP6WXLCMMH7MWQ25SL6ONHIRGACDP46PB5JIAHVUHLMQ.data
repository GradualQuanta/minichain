
ÑÉ#

# /!\ note the same IPFS node, but the default user's one
export IPFS_PATH=$HOME/.ipfs
pv=$(ipfs add -Q -n https://raw.githubusercontent.com/Gradual-Quanta/minichain/master/install.sh --progress=false)
ipfs pin rm $pv
qm=$(ipfs add -Q install.sh --progress=false --pin=true)
echo qm: $qm

cat > _data/install.yml <<EOF
--- # install scripts, command line
pk: QmcfHufAK9ErQ9ZKJF7YX68KntYYBJngkGDoVKcZEJyRve
date: $(date)
pv: $pv
qm: $qm
cmd: "curl https://ipfs.blockringâ„¢.ml/ipfs/$qm | sh /dev/stdin"
EOF
git add _data/install.yml

echo url: https://ipfs.blockringâ„¢.ml/ipfs/$qm 
É